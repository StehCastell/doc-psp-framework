# 🧪 Example — Cyclic Process (PSP3)

Scale PSP to larger projects in iterations.

## What this example includes
- Per-iteration plan and estimates
- Per-cycle reviews
- Iteration reports
- Cross-cycle consolidation

## When to use it as a reference
To apply PSP to large projects.

> Illustrative example of typical artifacts at this stage. Combine with `base-templates/`.
